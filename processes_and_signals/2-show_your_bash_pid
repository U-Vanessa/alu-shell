#!/bin/bash

# Get the PID of the current Bash process
pid=$PPID

# Iterate over all processes and print lines containing the Bash word
for line in $(ps -ax); do
  if [[ "$line" =~ "bash" ]] && [[ "$line" =~ "$pid" ]]; then
    echo "$line"
  fi
done 
